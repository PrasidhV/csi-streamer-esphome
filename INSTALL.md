================================================================================
CSI STREAMER COMPONENT - FILES TO COPY TO HA HOST
================================================================================

STEP 1: Create the component directory on the HA host
-----------------------------------------------------
Run this command on the HA host (or through the ESPHome dashboard terminal):

  mkdir -p /config/esphome/components/csi_streamer

STEP 2: Copy the following files to /config/esphome/components/csi_streamer/
-----------------------------------------------------------------------------

FILE 1: __init__.py
================================================================================
